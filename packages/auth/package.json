{
  "name": "@dwete/auth",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "lint": "biome check --write",
    "format": "biome format --write"
  },
  "dependencies": {
    "@dwete/db": "workspace:*",
    "@better-auth/expo": "latest",
    "better-auth": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@dwete/biome": "workspace:*",
    "@dwete/email": "workspace:*",
    "prettier": "latest",
    "typescript": "latest"
  }
}
